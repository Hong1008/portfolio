# AGENTS.md

## 1. Purpose

이 저장소는 소프트웨어 엔지니어의 경력과 프로젝트를 장기적으로 관리하는 정적 포트폴리오 사이트다.

새로운 회사 경력, 프로젝트, 기술 글과 실험 결과를 기존 구조를 변경하지 않고 추가할 수 있어야 한다.

에이전트의 역할은 다음과 같다.

* 제공된 근거를 바탕으로 경력 및 프로젝트 콘텐츠 작성
* 문제 해결 과정과 기술적 판단을 구조화
* 콘텐츠를 재사용 가능한 페이지와 컴포넌트로 구현
* 웹과 PDF에서 읽을 수 있는 시각적 결과물 생성
* 근거 없는 주장과 과장된 표현 검출
* 새로운 콘텐츠 추가 시 기존 페이지의 회귀 방지

이 사이트는 특정 기업의 지원동기나 채용공고에 종속된 문서를 만들지 않는다.

---

## 2. Core Principles

모든 콘텐츠는 다음 질문에 답해야 한다.

1. 어떤 맥락에서 발생한 문제인가?
2. 사용자의 역할과 책임 범위는 어디까지인가?
3. 어떤 제약 조건이 있었는가?
4. 원인을 어떻게 파악했는가?
5. 어떤 선택지를 검토했는가?
6. 왜 해당 구조 또는 기술을 선택했는가?
7. 무엇을 구현했는가?
8. 결과를 어떻게 검증했는가?
9. 어떤 결과를 얻었는가?
10. 해결하지 못한 한계는 무엇인가?

기술 이름을 나열하는 것보다 문제, 판단, 검증과 한계를 우선한다.

---

## 3. Separation of Responsibilities

### AGENTS.md

다음 내용만 관리한다.

* 작업 절차
* 콘텐츠 작성 규칙
* 사실 검증 규칙
* 코드와 디자인 원칙
* 완료 조건

특정 회사명, 프로젝트명, 기술 스택, 성과 수치와 작업 우선순위는 기록하지 않는다.

### Content Files

각 경력과 프로젝트의 실제 내용을 관리한다.

포함 가능한 내용:

* 제목
* 기간
* 조직 또는 프로젝트 형태
* 팀 구성
* 역할
* 기술
* 배경
* 문제
* 제약
* 의사결정
* 구현
* 검증
* 결과
* 한계
* 관련 링크

### Evidence Files

주장의 근거를 관리한다.

포함 가능한 내용:

* 저장소와 파일 경로
* 벤치마크 문서
* 테스트 결과
* 기술 회고
* 사용자가 직접 확인한 사실
* 공개 가능 여부
* 수치의 측정 환경
* 팀과 개인의 기여 범위

### Task Files

현재 구현할 범위와 순서를 관리한다.

AGENTS.md에 특정 페이지의 구현 순서를 고정하지 않는다.

---

## 4. Source Priority

사실관계가 충돌할 경우 다음 우선순위를 적용한다.

1. 사용자가 직접 확인한 최신 사실
2. 현재 실행되는 코드
3. 최신 공식 문서와 테스트 결과
4. 최신 이력서 또는 경력기술서
5. 당시 작성한 회고와 설계 문서
6. 저장소 README
7. 과거 문서와 초안

확인할 수 없는 내용은 추론하여 채우지 않는다.

불확실한 정보는 다음 중 하나로 처리한다.

* 콘텐츠에서 제외
* 불확실한 범위가 드러나도록 표현
* `TODO: 사실 확인 필요` 표시
* 사용자 확인 요청 목록에 기록

---

## 5. Factuality Rules

다음 행위를 금지한다.

* 근거 없는 성능 수치 추가
* 팀의 결과를 개인 단독 성과로 표현
* 회사 업무와 개인 프로젝트의 결과 혼합
* 재현 환경의 결과를 실제 운영 성과로 표현
* 출시되지 않은 시스템을 운영 서비스처럼 표현
* 사용하지 않은 기술을 기술 스택에 추가
* 실패 탐지 기능을 자동 복구로 과장
* 스케줄러 중복 방지를 전체 시스템의 멱등성으로 표현
* 모델 평가 지표를 사용자 또는 비즈니스 성과로 표현
* 테스트 대역의 출력을 실제 실행 결과로 표현
* 제안하거나 검토한 구조를 구현한 구조처럼 표현
* LLM이 생성한 아이디어를 사용자의 경험으로 귀속
* 보안과 개인정보 경험을 근거 없이 추가
* 지원한 기능과 안전하게 보장하는 기능을 혼동

수치를 사용할 때는 가능한 범위에서 다음을 함께 작성한다.

* 측정 대상
* 측정 환경
* 데이터 규모
* 비교 기준
* 측정 시점
* 결과의 적용 범위

---

## 6. Contribution Boundaries

모든 사례에서 개인과 팀의 역할을 구분한다.

다음 정보를 가능한 범위에서 기록한다.

* 전체 팀 구성
* 사용자의 담당 영역
* 다른 직군 또는 구성원이 담당한 영역
* 사용자가 직접 설계한 부분
* 사용자가 구현만 담당한 부분
* 기존 구조를 사용한 부분
* 외부 라이브러리 또는 서비스가 보장한 부분

`설계`, `주도`, `구축`, `개선` 등의 표현은 실제 책임 범위가 확인된 경우에만 사용한다.

---

## 7. Content Types

콘텐츠는 최소한 다음 유형을 지원해야 한다.

### Experience

회사 또는 조직에서 수행한 업무를 기록한다.

특징:

* 실제 운영 여부를 명시
* 조직 및 팀의 제약을 포함
* 내부 코드를 공개하지 않고 구조를 재구성할 수 있음
* 수치가 없다면 구조적 개선과 책임 범위를 중심으로 작성
* 개인 프로젝트에서 후속 구현한 내용을 혼합하지 않음

### Project

개인 또는 팀 프로젝트를 기록한다.

특징:

* 저장소와 실행 결과 연결
* 본인 기여 범위 명시
* 실험 및 측정 과정을 상세히 공개 가능
* 현재 상태와 남은 한계 포함
* README의 홍보 표현보다 실제 코드와 테스트를 우선

### Article

기술 글이나 회고를 기록한다.

특징:

* 특정 사례와 연결 가능
* 본문을 중복 저장하지 않고 원문 링크 제공 가능
* 포트폴리오 주장의 보조 근거로 활용

새로운 콘텐츠 유형이 추가되더라도 기존 유형의 스키마를 무리하게 확장하지 않는다.

---

## 8. Content Schema

경력과 프로젝트는 공통 메타데이터를 가진다.

```yaml
title:
slug:
contentType:
summary:
startDate:
endDate:
status:
organization:
projectType:
team:
role:
technologies:
featured:
visibility:
links:
evidence:
```

본문은 다음 섹션을 선택적으로 지원한다.

```yaml
context:
problem:
constraints:
hypotheses:
alternatives:
decisions:
architecture:
implementation:
verification:
results:
limitations:
learnings:
```

모든 섹션을 강제로 채우지 않는다.

사례의 성격에 따라 필요한 섹션만 사용하되, `summary`, `role`, `problem` 또는 `context`, `results` 또는 `learnings`는 반드시 포함한다.

---

## 9. Writing Workflow

새로운 경력 또는 프로젝트 콘텐츠는 다음 절차로 작성한다.

### Step 1. 자료 수집

가능한 자료를 확인한다.

* 이력서
* 저장소
* 코드
* README
* 테스트
* 벤치마크
* 회고
* 사용자 설명
* 공개 링크

### Step 2. 사실 목록 작성

문장을 작성하기 전에 확인된 사실을 분리한다.

```text
확인된 사실
확인되지 않은 주장
개인 기여
팀 기여
공개 가능한 내용
공개하면 안 되는 내용
사용 가능한 수치
수치의 측정 조건
```

### Step 3. 핵심 메시지 선정

한 사례에서 전달할 핵심 메시지는 1~2개로 제한한다.

예시 범주:

* 실패 처리
* 상태 관리
* 성능 분석
* 시스템 분리
* 데이터 품질
* 실험 설계
* AI 시스템 경계
* 운영 자동화
* 레거시 개선

모든 역량을 하나의 사례에 넣지 않는다.

### Step 4. 사례 유형 선택

사례에 맞는 서술 구조를 선택한다.

운영 문제:

```text
현상 → 원인 가설 → 확인 → 개선 → 남은 한계
```

신규 시스템:

```text
기존 문제 → 제약 → 대안 → 구조 선택 → 구현 범위
```

성능 문제:

```text
병목 가설 → 측정 환경 → 변경 → 재측정 → 트레이드오프
```

AI·데이터 실험:

```text
문제 정의 → 데이터 또는 모델 가설 → 실험 → 평가 → 채택·보류·폐기
```

라이브러리:

```text
사용자 문제 → API·추상화 설계 → 제약 정책 → 벤치마크 → 호환성 한계
```

### Step 5. 30초 요약 작성

상세 본문보다 먼저 다음 내용을 작성한다.

* 해결한 문제
* 가장 중요한 판단
* 본인의 역할
* 검증 또는 결과
* 대표적인 한계

### Step 6. 상세 본문 작성

확인된 사실만 사용해 본문을 작성한다.

### Step 7. 시각화 설계

글을 그대로 도식화하지 않는다.

시각화가 다음 중 하나를 더 잘 설명할 때만 추가한다.

* 시스템 경계
* 상태 전이
* 요청 흐름
* 데이터 흐름
* 선택지 비교
* 전후 성능
* 실험 결과
* 책임 분리

### Step 8. Claim Audit

모든 핵심 문장을 근거와 대조한다.

### Step 9. 웹·모바일·인쇄 검증

상세 페이지와 PDF 출력 결과를 모두 확인한다.

---

## 10. Writing Style

구체적이고 절제된 한국어를 사용한다.

### 권장 문장

```text
배치 재실행과 메시지 중복 전달을 고려해 발송 기준일과 트랜잭션 키에 DB UNIQUE 제약을 적용했다.
```

```text
네이티브 API 대비 DSL의 추가 비용을 확인하기 위해 동일한 데이터 규모와 JVM 설정으로 JMH 벤치마크를 구성했다.
```

### 피해야 할 문장

```text
안정적이고 확장 가능한 시스템을 성공적으로 구축했다.
```

```text
최신 기술을 적극 활용해 성능을 획기적으로 개선했다.
```

다음 표현은 근거가 있을 때만 사용한다.

* 주도
* 고도화
* 최적화
* 실시간
* 대규모
* 무중단
* 자동 복구
* 안정적
* 확장 가능
* 고가용성
* 획기적
* 완벽한

추상적인 평가보다 행동과 구조를 작성한다.

---

## 11. Technical Decision Writing

기술 선택을 설명할 때 다음 구조를 사용한다.

```text
상황:
검토한 선택지:
선택:
선택 이유:
얻은 점:
포기하거나 감수한 점:
```

선택지를 실제로 검토하지 않았다면 가상의 대안을 만들지 않는다.

현재 관점에서 떠올린 개선안은 당시 의사결정과 분리한다.

```text
당시 구현:
현재 확인한 한계:
```

사용자가 직접 검토하지 않은 미래 설계는 포트폴리오 본문에 자동으로 추가하지 않는다.

---

## 12. Verification Writing

`테스트했다`, `검증했다`, `성능을 개선했다`는 표현에는 검증 방법을 포함한다.

검증 방법의 예:

* 단위 테스트
* 통합 테스트
* 회귀 테스트
* 실제 실행 trace
* 벤치마크
* 로그 분석
* 데이터베이스 상태 확인
* 장애 재현
* 샘플 데이터 비교
* 사용자 확인
* 정적 분석

검증할 수 없는 결과는 다음처럼 표현한다.

```text
구조를 분리했다.
중복 구현을 줄였다.
실패 상태를 구분할 수 있도록 했다.
```

다음처럼 수치나 효과를 만들어내지 않는다.

```text
운영 효율을 크게 향상했다.
유지보수 비용을 대폭 절감했다.
장애율을 낮췄다.
```

---

## 13. Limitations

모든 대표 사례에는 최소 하나의 한계를 포함한다.

한계의 예:

* 처리하지 못한 실패 시나리오
* 외부 시스템에 의존하는 부분
* 측정되지 않은 운영 효과
* 데이터 편향
* 테스트하지 못한 환경
* 추상화가 지원하지 않는 기능
* 초기 공개 버전의 호환성
* 팀 또는 일정으로 인해 제외한 범위
* 평가 지표가 나타내지 않는 것

한계는 자기비판을 위한 문장이 아니라, 시스템의 책임 범위를 명확히 하기 위한 문장으로 작성한다.

---

## 14. Progressive Disclosure

상세 정보를 삭제하지 말고 노출 순서를 나눈다.

### 항상 표시

* 제목과 요약
* 기간과 역할
* 핵심 문제
* 대표적인 기술적 판단
* 주요 구조 또는 다이어그램
* 결과 또는 검증
* 한계

### 필요할 때 펼쳐 표시

* 상세 상태 흐름
* 전체 대안 비교
* 코드 예시
* 측정 환경
* 테스트 데이터
* 실패한 실험
* 긴 회고
* 추가 근거

핵심 내용은 JavaScript가 없어도 읽을 수 있어야 한다.

---

## 15. Site Architecture

콘텐츠 개수가 늘어나도 페이지 파일을 수동으로 추가하지 않는 구조를 사용한다.

권장 라우팅:

```text
/
/experience
/experience/[slug]
/projects
/projects/[slug]
/articles
/articles/[slug]
```

콘텐츠 파일을 추가하면 목록, 상세 페이지, 관련 콘텐츠와 검색 인덱스가 자동으로 갱신되어야 한다.

회사별 또는 기술별 경로를 코드에 하드코딩하지 않는다.

---

## 16. Component Architecture

컴포넌트는 특정 프로젝트에 종속되지 않아야 한다.

권장 컴포넌트 범주:

* ContentHeader
* SummaryCard
* MetadataList
* RoleBoundary
* ProblemStatement
* DecisionRecord
* ArchitectureDiagram
* ProcessFlow
* StateDiagram
* BenchmarkTable
* MetricComparison
* ExperimentRecord
* LimitationCallout
* EvidenceLinks
* RelatedContent
* ExpandableDetails

다음과 같은 컴포넌트를 만들지 않는다.

```text
SpecificProjectArchitecture
SpecificCompanyTimeline
SpecificBenchmarkResult
```

프로젝트별 차이는 콘텐츠 데이터와 슬롯을 통해 표현한다.

---

## 17. Visual System

디자인은 콘텐츠의 가독성과 기술적 관계 설명을 우선한다.

* 프로필 사진을 필수 요소로 사용하지 않는다.
* 밝은 배경과 한 가지 주 강조색을 사용한다.
* 상태, 경고, 성공을 의미하는 보조 색상은 제한적으로 사용한다.
* 본문 최대 폭을 제한한다.
* 모바일 우선으로 구현한다.
* 장식 목적의 애니메이션을 사용하지 않는다.
* 기술 로고를 반복적으로 나열하지 않는다.
* 긴 문단은 소제목, 표, 카드와 다이어그램으로 분리한다.
* 다이어그램 내부 텍스트는 본문보다 작아지지 않도록 한다.
* 색상 없이도 의미를 구분할 수 있어야 한다.

---

## 18. Diagram Rules

다이어그램은 콘텐츠 파일의 구조화된 데이터나 범용 문법으로 관리한다.

가능한 방식:

* Mermaid
* 구조화된 노드·엣지 데이터
* 재사용 가능한 SVG 컴포넌트

프로젝트마다 편집 불가능한 이미지로만 저장하지 않는다.

다이어그램을 추가할 때 다음을 확인한다.

* 본문을 읽지 않아도 핵심 흐름을 이해할 수 있는가?
* 컴포넌트와 책임 경계가 구분되는가?
* 정상 흐름과 실패 흐름이 혼동되지 않는가?
* 모바일에서 읽을 수 있는가?
* PDF 출력에서 잘리지 않는가?
* 실제 구현보다 더 많은 구성 요소를 그리지 않았는가?

---

## 19. Charts and Metrics

수치 비교에는 표 또는 간단한 차트를 사용한다.

차트를 사용할 조건:

* 비교 대상이 두 개 이상
* 순서나 차이가 핵심 메시지
* 측정 조건을 함께 제시할 수 있음

차트에는 다음을 포함한다.

* 지표명
* 단위
* 비교 기준
* 측정 환경 또는 링크
* 수치가 의미하는 범위

축을 잘라 차이를 과장하지 않는다.

서로 다른 단위의 수치를 하나의 축에 배치하지 않는다.

---

## 20. Evidence Management

모든 대표 주장에는 근거를 연결할 수 있어야 한다.

권장 evidence 구조:

```yaml
evidence:
  - type: repository
    url:
  - type: source
    path:
  - type: benchmark
    path:
  - type: retrospective
    url:
  - type: user-confirmed
    note:
```

웹 페이지에는 공개 가능한 근거만 노출한다.

비공개 자료는 콘텐츠 작성과 검증에만 사용하고 URL이나 원문을 노출하지 않는다.

---

## 21. Public and Private Information

다음 내용은 공개 전 확인한다.

* 회사 내부 코드
* 고객 또는 사용자 데이터
* 접근키와 환경 변수
* 내부 도메인과 IP
* 실제 결제 식별자
* 비공개 API 명세
* 계약상 공개할 수 없는 아키텍처
* 개인정보
* 팀원의 실명과 개인 연락처

회사 사례의 재구성 다이어그램에는 필요한 경우 다음 안내를 표시한다.

> 내부 코드와 데이터는 포함하지 않았으며, 공개 가능한 범위에서 당시 처리 구조를 재구성했습니다.

---

## 22. Related Content

콘텐츠 간 연결은 기술 태그만으로 결정하지 않는다.

다음 관계를 지원한다.

* 실무 문제와 후속 개인 프로젝트
* 프로젝트와 기술 회고
* 프로젝트와 벤치마크
* 비슷한 실패 처리 사례
* 동일한 설계 원칙을 사용한 사례
* 학습 내용과 실제 구현

관련 콘텐츠는 명시적 관계와 태그를 함께 사용한다.

---

## 23. Technology Constraints

정적 생성 방식과 콘텐츠 중심 구조를 우선한다.

권장 조건:

* 정적 사이트 생성
* 파일 기반 콘텐츠 관리
* 타입 또는 스키마 검증
* 최소한의 클라이언트 JavaScript
* GitHub Pages 호환
* 자동 빌드와 배포
* 인쇄 전용 스타일
* 접근성 검사
* 링크 검사

콘텐츠를 페이지 컴포넌트에 하드코딩하지 않는다.

프레임워크 변경이 필요할 경우 콘텐츠 파일의 마이그레이션 비용을 우선 고려한다.

---

## 24. PDF Export

HTML을 원본으로 관리한다.

PDF는 동일한 콘텐츠에서 생성하되, 웹의 모든 상세 내용을 그대로 출력하지 않는다.

콘텐츠에는 PDF 노출 수준을 지정할 수 있어야 한다.

```yaml
print:
  include: true
  priority:
  detailLevel:
```

PDF 생성 시 다음을 확인한다.

* 페이지 분할
* 표와 다이어그램 잘림
* 링크 표시
* 글자 크기
* 불필요한 내비게이션 제거
* 접힌 세부정보 제외
* 핵심 사례 우선 배치

웹과 PDF에서 서로 다른 사실이 표시되지 않아야 한다.

---

## 25. Quality Checks

콘텐츠 또는 코드를 변경한 뒤 다음 검사를 수행한다.

### Content

* 필수 필드 스키마 검증
* 날짜 형식 검증
* 중복 slug 검증
* 깨진 링크 검사
* 근거 없는 수치 탐지
* 금지 표현 탐지
* 팀과 개인 역할 구분
* 공개 범위 확인

### UI

* 모바일 레이아웃
* 데스크톱 레이아웃
* 키보드 탐색
* 색상 대비
* 헤딩 순서
* 이미지 대체 텍스트
* 다이어그램 가독성
* 긴 코드와 표의 오버플로

### Build

* 정적 빌드
* GitHub Pages base path
* 자산 경로
* 404 페이지
* sitemap
* robots
* 인쇄 스타일
* PDF 생성

---

## 26. Claim Audit

각 콘텐츠의 핵심 주장을 다음 표로 검토한다.

```text
주장:
주장 유형:
근거:
본인 기여:
측정 환경:
공개 가능 여부:
과장 위험:
최종 표현:
```

다음 질문에 하나라도 답할 수 없으면 표현을 수정하거나 제외한다.

* 사용자가 직접 수행한 일인가?
* 팀의 역할과 구분되는가?
* 실제 코드나 확인된 설명과 일치하는가?
* 수치의 출처가 있는가?
* 당시 구현과 현재 의견이 분리되는가?
* 결과가 나타내는 범위를 설명할 수 있는가?
* 한계가 숨겨져 있지 않은가?

---

## 27. Extensibility Requirements

새로운 콘텐츠를 추가할 때 다음 작업만 필요해야 한다.

1. 콘텐츠 파일 생성
2. 근거 정보 연결
3. 필요한 다이어그램 데이터 추가
4. 자산 추가
5. 검증 실행

새 콘텐츠를 추가하기 위해 다음을 수정하지 않아야 한다.

* 공통 라우팅 코드
* 내비게이션의 고정 목록
* 상세 페이지 컴포넌트
* 홈 페이지의 수동 카드 배열
* PDF 페이지별 하드코딩
* 관련 콘텐츠 목록

새로운 표현 방식이 필요하면 특정 사례 전용 컴포넌트보다 범용 컴포넌트로 일반화할 수 있는지 먼저 검토한다.

---

## 28. Definition of Done

콘텐츠 하나가 완료되려면 다음 조건을 만족해야 한다.

* 상단 요약만으로 문제와 역할을 이해할 수 있다.
* 개인과 팀의 기여가 구분된다.
* 기술적 판단이 최소 하나 포함된다.
* 검증 방법 또는 확인 가능한 결과가 포함된다.
* 대표 사례라면 한계가 포함된다.
* 사용한 수치에 근거가 있다.
* 공개 범위가 확인됐다.
* 모바일과 데스크톱에서 읽을 수 있다.
* PDF 출력에서 핵심 내용이 잘리지 않는다.
* 관련 링크가 동작한다.
* 콘텐츠 스키마 검증을 통과한다.
* Claim Audit을 통과한다.
* 기존 콘텐츠와 빌드에 회귀가 없다.

---

## 29. Agent Behavior

에이전트는 작업 중 다음 원칙을 따른다.

* 자료를 먼저 읽고 문장을 작성한다.
* 확인되지 않은 사실을 자연스럽게 보이도록 채우지 않는다.
* 콘텐츠 구조가 비어 있다는 이유로 가상의 내용을 만들지 않는다.
* 기존 콘텐츠의 문체와 구성만 복제하지 않고 사례의 성격에 맞게 작성한다.
* 사용자의 실제 판단과 LLM의 제안을 구분한다.
* 큰 변경은 콘텐츠, UI, 빌드 단위로 분리한다.
* 생성한 코드와 콘텐츠를 직접 검증한다.
* 실패한 검사와 남은 TODO를 숨기지 않는다.
* 새로운 사례가 기존 구조에 맞지 않으면 콘텐츠를 왜곡하지 말고 스키마 확장을 검토한다.
* 스키마를 확장할 때 기존 콘텐츠와의 하위 호환성을 유지한다.
