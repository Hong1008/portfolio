---
title: "샤플앤컴퍼니"
slug: "shopl"
contentType: "experience"
summary: "배치 재실행과 메시지 중복 전달을 서로 다른 DB 제약으로 방어하는 메일 발송 구조를 구현했습니다. 점검 보고서의 엑셀 생성 방식을 전환하고 반복되는 생성 흐름과 라이브러리 종속 영역을 분리했습니다."

startDate: "2022-11"
endDate: "2023-08"
datePrecision: "month"
status: "completed"

organization:
  name: "(주)샤플앤컴퍼니"
  displayName: "샤플앤컴퍼니"
  url: null

employment:
  type: "full-time"
  department: "HADA 개발팀"
  position: "백엔드 개발자"

operationStatus: "production"

team:
  size: 6
  composition:
    - role: "Backend"
      count: 2
    - role: "Lead Developer (Full-stack)"
      count: 1
    - role: "Frontend"
      count: 1
    - role: "PM (Planning)"
      count: 1
    - role: "Planning"
      count: 1
  note: "경력에 나열한 구현은 모두 본인이 담당했으며, 업무별 기획자·프론트엔드 개발자와 협업"

role:
  title: "백엔드 개발"
  responsibilities:
    - "온보딩·인보이스 메일 발송 시스템 개발"
    - "점검 보고서 엑셀 생성 구조 개선"
    - "현장 이슈 관리 API 개발"
  contributionSummary: "나열한 세 업무의 구현을 모두 담당했으며, 업무별 기획자·프론트엔드 개발자와 협업했습니다."

technologies:
  primary:
    - "Kotlin"
    - "Spring Boot"
    - "Spring Batch"
    - "MySQL"
    - "AWS SQS"
    - "AWS SES"
  secondary:
    - "JPA"
    - "QueryDSL"
    - "FastExcel"
    - "Apache POI"
    - "AWS S3"

themes:
  - "reliability"
  - "idempotency"
  - "architecture"
  - "performance"
  - "maintenance"

featured: true
visibility: "public"
confidentialityNote: "회사 코드와 사용자·현장 데이터는 포함하지 않고 공개 가능한 범위에서 처리 구조를 재구성했습니다."

links:
  repository: null
  demo: null
  retrospective: "https://velog.io/@hong1008/remind-noti-service"
  article: null
  external:
    - label: "샤플 메인 프로덕트"
      url: "https://www.shoplworks.com/"
    - label: "HADA 팀"
      url: "https://www.hadaworks.com/"

cases:
  - id: "mail-delivery"
    title: "온보딩·인보이스 메일 발송 시스템"
    featured: true
  - id: "report-generation"
    title: "점검 보고서 생성 구조 개선"
    featured: true
  - id: "issue-management"
    title: "현장 이슈 관리"
    featured: false

evidenceRef: "shopl"

caseSummary:
  problem: "서버 일괄 메일의 대상 선정과 실제 전송을 분리하면서, 배치 재실행과 SQS 메시지 중복 전달을 각각 처리해야 했습니다."
  decision: "배치 서버와 메일 서버를 SQS로 분리하고 발송 요청과 메시지 소비 단계에 서로 다른 DB UNIQUE 제약을 적용했습니다."
  role: "메일 발송 구조와 중복 방어, Kotlin DSL 보고서 공통 빌더, 현장 이슈 관리 API를 직접 구현했습니다."
  verification: "배치 재실행과 메시지 재전달 시나리오를 분리해 각 UNIQUE 키와 방어 범위를 대조했습니다."
  limitation: "DB 제약은 확인된 두 단계의 중복 처리를 방어하지만 메일의 Exactly-once 전송이나 전체 실패 복구를 보장하지 않습니다."

limitations:
  - "두 DB UNIQUE 제약은 발송 요청 생성과 메시지 소비의 중복을 각각 방어하며, 메일의 Exactly-once 전송을 보장하지 않습니다."
  - "메일 재처리와 외부 전송 실패 복구의 전체 범위는 확인되지 않아 자동 복구로 표현하지 않습니다."
  - "보고서 생성 방식 전환의 운영 성능 수치는 원본 측정 근거가 확인되지 않아 구조적 변경만 기록합니다."
  - "KExcel의 엔진 추상화와 벤치마크는 이후 개인 프로젝트의 결과이며 회사 업무의 성과가 아닙니다."

print:
  include: true
  priority: 40
  detailLevel: "standard"
---

import CaseSection from "../../components/content/CaseSection.astro";
import IdempotencyLayers from "../../components/content/IdempotencyLayers.astro";
import ProcessFlow from "../../components/content/ProcessFlow.astro";
import RelatedCase from "../../components/content/RelatedCase.astro";
import RoleBoundary from "../../components/content/RoleBoundary.astro";

> 회사 내부 코드와 사용자·현장 데이터는 포함하지 않았으며, 공개 가능한 범위에서 당시 처리 구조와 책임 경계를 재구성했습니다.

## 경력 정보와 역할 경계

HADA 개발팀은 백엔드 개발자 2명, 리드 개발자(풀스택) 1명, 프론트엔드 개발자 1명, PM(기획) 1명과 기획자 1명으로 구성됐습니다. 이 페이지에 나열한 세 업무의 구현은 모두 직접 담당했으며, 업무별 기획자와 프론트엔드 개발자와 협업했습니다. 세 업무는 모두 운영됐습니다.

<RoleBoundary
  mine={[
    "온보딩·인보이스 메일의 대상 선정과 전송 기능 구현",
    "배치 재실행과 SQS 중복 전달에 대한 DB UNIQUE 제약 적용",
    "점검 보고서 생성 방식을 POI에서 FastExcel로 전환하고 Kotlin DSL 공통 빌더 구현",
    "현장 이슈의 상태·변경 이력과 S3 이미지 API 구현",
  ]}
  team={[
    "업무별 기획자·프론트엔드 개발자와 기능 요구사항 및 화면 연동 협업",
  ]}
  external={[
    "AWS SQS의 메시지 전달",
    "AWS SES의 실제 이메일 전송",
    "Apache POI와 FastExcel의 XLSX 생성 기능",
    "AWS S3의 첨부 이미지 저장",
  ]}
/>

<CaseSection
  id="mail-delivery"
  title="Case 1. 온보딩·인보이스 메일 발송 시스템"
  summary="대상 선정과 실제 메일 전송을 분리하고, 배치 재실행과 메시지 중복 전달을 서로 다른 키로 방어했습니다."
  featured={true}
>

### 문제를 나눈 기준

기존 네이버웍스 메일 API는 서버에서 메일을 일괄 발송하는 방식에 적합하지 않았습니다. 전송 수단을 바꾸는 것과 별개로 배치가 중단되거나 다시 실행될 수 있었고, SQS 메시지도 중복 전달될 수 있었습니다.

따라서 다음 문제를 하나의 중복 처리로 합치지 않았습니다.

1. 메일을 받을 대상을 결정하는 업무 조건
2. 동일 기준의 배치가 다시 실행되는 경우
3. 동일한 메시지가 메일 서버에 다시 전달되는 경우

### 대상 선정과 전송의 책임 분리

배치 서버는 발송 대상자를 선별하고 발송 요청을 기록한 뒤 SQS 메시지를 발행합니다. 메일 서버는 메시지를 소비해 중복 처리 여부를 확인하고 AWS SES로 메일을 전송합니다.

<ProcessFlow
  title="온보딩과 인보이스 메일 발송 책임 경계"
  description="배치 서버가 대상을 선정하고 요청을 기록한 뒤 SQS로 전달하면, 메일 서버가 트랜잭션 키를 기록하고 SES로 전송합니다."
  steps={[
    { label: "배치 서버", description: "발송 대상자 선정" },
    { label: "발송 요청 DB", description: "기준일과 대상 기준 기록" },
    { label: "AWS SQS", description: "메일 요청 메시지 전달" },
    { label: "메일 서버", description: "메시지 소비와 중복 확인" },
    { label: "메일 처리 DB", description: "메일 트랜잭션 키 기록" },
    { label: "AWS SES", description: "실제 이메일 전송" },
  ]}
  transitions={[
    "발송 요청 기록",
    "메시지 발행",
    "중복 전달 가능",
    "트랜잭션 기록",
    "전송 요청",
  ]}
/>

이 분리로 온보딩 상태를 추적하는 조건과 실제 메일 발송 조건을 구분했습니다. 같은 전송 구조는 인보이스 메일 발송에도 재사용했습니다.

### 서로 다른 두 중복 방어선

<IdempotencyLayers
  label="메일 발송의 단계별 중복 방어선"
  layers={[
    {
      name: "배치 재실행 방어",
      trigger: "동일 기준일의 배치가 다시 실행되는 경우",
      key: "발송 기준일과 대상 기준",
      boundary: "동일 대상의 발송 요청이 중복 생성되는 것을 방지",
    },
    {
      name: "메시지 중복 전달 방어",
      trigger: "동일 SQS 메시지가 메일 서버에 다시 전달되는 경우",
      key: "메일 트랜잭션 키",
      boundary: "메일 서버에서 동일 메시지를 중복 처리하는 것을 방지",
    },
  ]}
/>

두 제약은 서로 다른 시점의 중복을 방어합니다. 이 범위를 전체 시스템의 멱등성이나 Exactly-once 메일 전송 보장으로 확대하지 않습니다. 재처리와 외부 전송 실패 복구의 전체 범위도 확인되지 않아 자동 복구로 표현하지 않습니다.

### 구조적 결과

- 대상자 선별과 메일 전송의 책임을 배치 서버와 메일 서버로 분리했습니다.
- 배치 재실행과 메시지 중복 전달을 서로 다른 DB UNIQUE 제약으로 방어했습니다.
- 같은 발송 구조를 인보이스 메일에 재사용했습니다.

운영 발송량, 실패율과 메일 유실률은 공개 가능한 측정 근거가 없어 결과에 포함하지 않습니다.

</CaseSection>

<CaseSection
  id="report-generation"
  title="Case 2. 점검 보고서 생성 구조 개선"
  summary="엑셀 생성 방식을 FastExcel로 전환하고 보고서별 반복 흐름과 라이브러리 종속 영역을 공통화했습니다."
  featured={true}
>

### 문제와 변경

Apache POI 기반 점검 보고서 생성에서 지연이 발생했고, 보고서마다 Workbook·Sheet·Row·Cell을 다루는 코드가 반복됐습니다. 엑셀 라이브러리 API도 보고서 로직에 직접 노출돼 있었습니다.

생성 방식을 FastExcel 기반으로 전환하고 Kotlin DSL을 사용해 반복되는 생성 흐름을 공통 빌더로 분리했습니다. 보고서별 로직에서는 엑셀 라이브러리 종속 코드를 직접 다루지 않도록 공통 영역에 제한했습니다.

### 확인 가능한 결과

- POI 기반 생성 방식을 FastExcel 기반으로 전환했습니다.
- Kotlin DSL을 사용해 반복되는 보고서 생성 흐름을 공통 빌더로 분리했습니다.
- 보고서 로직과 엑셀 라이브러리 종속 영역을 구분했습니다.

당시 운영 환경의 원본 측정 근거가 확인되지 않아 생성 시간 개선 수치는 사용하지 않습니다. 회사에서 KExcel의 `ExcelDriver`, JMH 벤치마크나 동시 쓰기 Fail-Fast를 구현한 것으로도 표현하지 않습니다.

<RelatedCase
  label="KExcel — Kotlin DSL 기반 대용량 엑셀 생성 라이브러리"
  href="/projects/kexcel/"
  relationship="실무에서 경험한 반복 코드와 엔진 종속 문제를 이후 개인 프로젝트에서 엔진 추상화와 벤치마크까지 확장했습니다."
/>

</CaseSection>

<CaseSection
  id="issue-management"
  title="Case 3. 현장 이슈 관리"
  summary="현장 이슈의 등록과 처리 상태를 관리하고, 변경 이력과 다중 이미지 첨부 기능을 구현했습니다."
>

현장 작업자가 이슈를 등록하고 해결 과정을 추적할 수 있는 API를 개발했습니다. 상태와 변경 이력을 관리하고, AWS S3를 연동해 여러 이미지를 첨부하거나 수정할 수 있도록 했습니다.

확인된 범위는 상태 변경 이력과 이미지 처리이며, 워크플로 엔진이나 이벤트 소싱으로 확대하지 않습니다. 실제 고객사명, 현장명과 내부 상태 코드는 공개하지 않습니다.

</CaseSection>

## 근거

- 사용자가 확인한 역할과 공개 범위
- 배치 서버·메일 서버와 SQS·SES의 책임 경계
- 발송 기준일과 메일 트랜잭션 키에 적용한 DB UNIQUE 제약의 범위
- 보고서 생성 방식 전환과 공통 빌더 구현 범위
- 현장 이슈 상태·변경 이력과 S3 이미지 처리 범위
